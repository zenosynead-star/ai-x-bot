# Streamlit Secrets
# ローカル実行時はこのファイルを secrets.toml にリネームして実値を入れる
# Streamlit Cloud は Settings → Secrets に同じ内容を貼り付け

# GitHub Personal Access Token
# 必要な権限: repo (workflow_dispatch / artifact取得用)
# 発行: https://github.com/settings/tokens/new
GITHUB_TOKEN = "ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxx"
